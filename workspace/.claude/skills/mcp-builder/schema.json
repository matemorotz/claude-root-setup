{
  "skill": "mcp-builder",
  "version": "1.0.0",
  "input": {
    "required": {
      "task": "string — plain language description (e.g. 'build MCP server for GitHub API', 'add file-read tool to existing server')"
    },
    "optional": {
      "server_name": "string — name for the MCP server",
      "transport": "stdio|http|sse — transport protocol",
      "language": "python|typescript — implementation language",
      "output_dir": "path — where to create server files"
    },
    "context": {
      "project": "string",
      "parent_task": "string",
      "dependencies": ["skill-name"]
    }
  },
  "output": {
    "status": "success|error|dependency-needed",
    "result": "any — server summary or implementation details",
    "dependency_requests": [{"skill": "string", "task": "string", "reason": "string"}],
    "files_created": ["path"],
    "error": "string if status=error"
  }
}